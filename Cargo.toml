[package]
name = "NSFW"
version = "0.1.0"
edition = "2024"

[dependencies]
openlb = { version = "0.1", features = ["text_scan"] }
anyhow = "1.0"

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = "z"
